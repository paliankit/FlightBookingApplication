package com.flightapp.user.service.services;

import org.springframework.stereotype.Service;

@Service
public class MovieService {

	/*
	 * @Autowired private MovieRepository repo;
	 * 
	 * public List<Movie> getAllMovies(){ // logic return repo.findAll(); }
	 * 
	 * public Movie getMovie(@PathVariable int id){ // logic
	 * 
	 * Optional<Movie> optional = repo.findById(id); if(optional.isPresent()) {
	 * return optional.get(); } else { // throw exception
	 * System.out.println("Movie with id "+id+" is not found in db"); return null; }
	 * }
	 * 
	 * public Movie createMovie(@RequestBody Movie m){ // logic return repo.save(m);
	 * }
	 */

}
